% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_oncoprint.R
\name{plot_oncoprint}
\alias{plot_oncoprint}
\alias{plot_waterfall}
\title{Oncoprint / waterfall plot of variants across samples}
\usage{
plot_oncoprint(
  variants,
  top_n = NULL,
  genes = NULL,
  samples = NULL,
  annotation = NULL,
  palette = NULL,
  interactive = FALSE
)

plot_waterfall(
  variants,
  top_n = NULL,
  genes = NULL,
  samples = NULL,
  annotation = NULL,
  palette = NULL,
  interactive = FALSE
)
}
\arguments{
\item{variants}{A \code{gvf} object from \code{\link[=read_vcf]{read_vcf()}} or \code{\link[=coerce_variants]{coerce_variants()}},
or any \code{data.frame} with \code{gene}, \code{sample}, and \code{consequence} columns.}

\item{top_n}{Integer. Show the \code{top_n} most frequently altered genes.
Mutually exclusive with \code{genes}; if both are \code{NULL}, defaults to \code{10}.}

\item{genes}{Character vector of specific genes to show, in place of
\code{top_n}. Mutually exclusive with \code{top_n}. Genes not present in
\code{variants} are shown as fully unaltered rows.}

\item{samples}{Character vector of sample names to include as columns.
\code{NULL} (default) uses every sample present in \code{variants}.}

\item{annotation}{Optional \code{data.frame} of sample-level metadata drawn as
annotation tracks below the mutation matrix. Must contain a \code{sample}
column matching sample identifiers in \code{variants}, plus one or more
additional columns to display, one per track. \code{NULL} (default) omits
annotation tracks.}

\item{palette}{Named character vector of colours keyed by consequence.
\code{NULL} uses the built-in \code{gv_palette("consequence")}. If it does not
already contain a \code{"Multi_Hit"} entry, one is added automatically.}

\item{interactive}{Logical. Returns a \code{plotly} object if \code{TRUE}.}
}
\value{
A \code{ggplot} object (or a \code{plotly} object when \code{interactive = TRUE}).
}
\description{
Draws a gene-by-sample mutation matrix ("oncoprint" in the
\code{ComplexHeatmap}/cBioPortal vocabulary, "waterfall plot" in the
\code{GenVisR}/\code{maftools} vocabulary — the same visualisation under two
names). \code{plot_waterfall()} is an alias for \code{plot_oncoprint()}; both
produce identical output.
}
\details{
\strong{Gene order.} Genes are ranked by the number of distinct samples
carrying at least one mutation in that gene (ties broken by total
mutation count, then alphabetically), and the \code{top_n} most frequently
altered genes are shown, most-altered at the top.

\strong{Sample order.} Samples are ordered using the memo-sort ("cascade")
algorithm: the displayed genes, already ranked most-to-least altered,
are treated as bits of a binary number (the most-altered gene the most
significant bit). Each sample's mutation pattern across the displayed
genes is scored as that binary number, and samples are sorted by
descending score (ties broken alphabetically by sample name). This
greedily groups samples that share mutations in the top genes together,
producing the characteristic left-to-right staircase pattern. Only the
displayed genes contribute to the score; genes excluded by \code{top_n}/
\code{genes} have no effect on sample order.

\strong{Multi-hit cells.} A gene mutated more than once in the same sample
cannot be represented by a single consequence colour, so it is shown as
a distinct \code{"Multi_Hit"} category instead of either consequence.
}
\examples{
vcf_file <- system.file("extdata", "example.vcf", package = "ggvariant")
variants <- read_vcf(vcf_file)

# Top 5 most-altered genes
plot_oncoprint(variants, top_n = 5)

# A specific gene panel instead of top_n
plot_oncoprint(variants, genes = c("TP53", "BRCA1", "BRCA2"))

# With a clinical annotation track
clinical <- data.frame(
  sample = c("TUMOR_S1", "TUMOR_S2"),
  stage  = c("III", "IV")
)
plot_oncoprint(variants, top_n = 5, annotation = clinical)

}
\references{
Gao J, Aksoy BA, Dogrusoz U, et al. (2013). Integrative analysis of
complex cancer genomics and clinical profiles using the cBioPortal.
\emph{Science Signaling}, 6(269), pl1. \doi{10.1126/scisignal.2004088}

Skidmore ZL, Wagner AH, Lesurf R, et al. (2016). GenVisR: Genomic
Visualizations in R. \emph{Bioinformatics}, 32(19), 3012-3014.
\doi{10.1093/bioinformatics/btw325}

Gu Z, Eils R, Schlesner M (2016). Complex heatmaps reveal patterns and
correlations in multidimensional genomic data. \emph{Bioinformatics},
32(18), 2847-2849. \doi{10.1093/bioinformatics/btw313}

Mayakonda A, Lin DC, Assenov Y, Plass C, Koeffler HP (2018). Maftools:
efficient and comprehensive analysis of somatic variants in cancer.
\emph{Genome Research}, 28(11), 1747-1756. \doi{10.1101/gr.239244.118}
}
\seealso{
\code{\link[=plot_tmb]{plot_tmb()}}, \code{\link[=plot_consequence_summary]{plot_consequence_summary()}}, \code{\link[=gv_palette]{gv_palette()}}

Other ggvariant plots: 
\code{\link{plot_consequence_summary}()},
\code{\link{plot_lollipop}()},
\code{\link{plot_tmb}()},
\code{\link{plot_variant_spectrum}()}
}
\concept{ggvariant plots}
