% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tmb.R
\name{plot_tmb}
\alias{plot_tmb}
\title{Tumour mutational burden bar chart}
\usage{
plot_tmb(
  variants,
  samples = NULL,
  mb_size = NULL,
  sample_order = NULL,
  palette = NULL,
  interactive = FALSE
)
}
\arguments{
\item{variants}{A \code{gvf} object from \code{\link[=read_vcf]{read_vcf()}} or \code{\link[=coerce_variants]{coerce_variants()}},
or any \code{data.frame} with \code{sample} and \code{pos} columns.}

\item{samples}{Character vector of sample names to include. \code{NULL}
(default) uses every sample present in \code{variants}.}

\item{mb_size}{Numeric. Size, in megabases, of the sequenced region used
to normalise mutation counts into mutations/Mb. \code{NULL} (default) plots
raw mutation counts instead.}

\item{sample_order}{Character vector giving an explicit left-to-right
sample order, e.g. to align with \code{\link[=plot_oncoprint]{plot_oncoprint()}}'s memo-sorted
column order. \code{NULL} (default) orders samples by descending TMB.}

\item{palette}{Single hex colour string for the bars. \code{NULL} uses a
built-in default.}

\item{interactive}{Logical. Returns a \code{plotly} object if \code{TRUE}.}
}
\value{
A \code{ggplot} object (or a \code{plotly} object when \code{interactive = TRUE}).
}
\description{
Plots per-sample tumour mutational burden (TMB): the number of mutations
carried by each sample, optionally normalised to mutations per megabase.
}
\details{
The per-sample mutation count is computed by an internal helper shared
with any future \code{\link[=plot_oncoprint]{plot_oncoprint()}} TMB marginal, so the two always agree.
Pass \code{mb_size} (the size, in megabases, of the sequenced region) to
convert raw counts into mutations/Mb, the conventional TMB unit; leave it
\code{NULL} to plot raw counts.
}
\examples{
vcf_file <- system.file("extdata", "example.vcf", package = "ggvariant")
variants <- read_vcf(vcf_file)

# Raw mutation counts, samples ordered by descending TMB
plot_tmb(variants)

# Normalised to mutations/Mb for a 38 Mb exome
plot_tmb(variants, mb_size = 38)

# Aligned to a specific sample order, e.g. from plot_oncoprint()
plot_tmb(variants, sample_order = c("TUMOR_S1", "TUMOR_S2"))

}
\references{
Chalmers ZR, Connelly CF, Fabrizio D, et al. (2017). Analysis of
100,000 human cancer genomes reveals the landscape of tumor
mutational burden. \emph{Genome Medicine}, 9(1), 34.
\doi{10.1186/s13073-017-0424-2}
}
\seealso{
\code{\link[=plot_oncoprint]{plot_oncoprint()}}, \code{\link[=gv_palette]{gv_palette()}}

Other ggvariant plots: 
\code{\link{plot_consequence_summary}()},
\code{\link{plot_lollipop}()},
\code{\link{plot_oncoprint}()},
\code{\link{plot_variant_spectrum}()}
}
\concept{ggvariant plots}
